interface:
  display_name: "Codex Dispatch"
  short_description: "Dispatch Codex CLI as worker, verifier, reviewer, or synthesizer."
  default_prompt: "Use codex-dispatch for this task."
policy:
  allow_implicit_invocation: true
